# Zane Hollis WB1 — exact archived source

`ZANE_HOLLIS_SOLO_WB1_CORE_WORLD_RUNTIME_PATCHED.json` is stored here as four byte-preserving sequential text parts because the connected GitHub writer could not safely send the full 44 KB JSON in one file operation.

The four files are exact consecutive slices of the approved final WB1. They were split without adding separators or changing content.

## Reassemble

From this directory:

```bash
cat part-01.txt part-02.txt part-03.txt part-04.txt > ../ZANE_HOLLIS_SOLO_WB1_CORE_WORLD_RUNTIME_PATCHED.json
```

The resulting file is the approved runtime WB1.

Expected source size: **44,002 bytes**.

Expected Git blob SHA for the reconstructed file: `3a6791a7b815b48d93fa529fa63c4fb15d0ebe8c`.

Parts:
- `part-01.txt`
- `part-02.txt`
- `part-03.txt`
- `part-04.txt`
